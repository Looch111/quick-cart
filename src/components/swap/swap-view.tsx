"use client";

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function SwapView() {
  return (
    <main className="flex-1 space-y-6 p-4 lg:p-6">
      <Card>
        <CardHeader>
          <CardTitle>Swap Crypto</CardTitle>
          <CardDescription>This feature is being temporarily simplified to resolve a critical build error.</CardDescription>
        </CardHeader>
        <CardContent>
          <p>The swap functionality will be restored shortly.</p>
        </CardContent>
      </Card>
    </main>
  );
}
